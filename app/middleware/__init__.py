"""Middleware that wraps tool execution.

Slice 6 will add `confirm` (gates dangerous tools) and `history` (revert log).
Slice 5 will add `narrate` (post-action describe_focus).

For now this is a placeholder package marker.
"""
